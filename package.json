{
  "name": "fulebuddy",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "docker-compose up --build",
    "stop": "docker-compose down"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
} 